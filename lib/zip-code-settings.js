var configurationSettings = require("configuration-settings.js");

var mongoDB = require('mongodb');
var mongoDBClient = mongoDB.MongoClient;
var mongoDBUrl = configurationSettings.mongoDBUrl;

function get(companyId, product, zip)
{

}

function create(companyId, product, zip, settings)
{

}

function update(companyId, product, zip, settings)
{
  
}
